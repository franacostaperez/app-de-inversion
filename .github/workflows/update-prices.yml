name: Refresh prices and trailing PE

on:
  workflow_dispatch:
  schedule:
    - cron: "17 7,19 * * *"

permissions:
  contents: write

jobs:
  update:
    uses: ./.github/workflows/data-pipeline.yml
    with:
      mode: "prices"
    secrets: inherit
